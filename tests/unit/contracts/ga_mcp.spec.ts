import { describe, it } from "vitest";

describe.skip("GA MCP contract — staging parity", () => {
  it("aligns GA facts between Supabase mirror and MCP live host", () => {
    // TODO: Implement once OCC-INF-221 delivers staging SUPABASE credentials and live MCP endpoint.
  });
});
