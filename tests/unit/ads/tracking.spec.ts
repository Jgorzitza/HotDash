// Compatibility shim: preserve legacy contract path expected by directions/CI
// Runs current ads unit specs so contract path remains valid during refactors.
import "./attribution.spec";
import "./pacing.spec";
