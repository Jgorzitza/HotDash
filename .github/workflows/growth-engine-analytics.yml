name: Growth Engine - Analytics Agent (Daily)

on:
  schedule:
    # Run daily at 3 AM UTC
    - cron: '0 3 * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  analytics-agent:
    runs-on: ubuntu-latest
    
    steps:
      - name: Trigger Analytics Agent
        run: |
          curl -X POST "${{ secrets.APP_URL }}/api/cron/growth-engine/analytics" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            -H "Content-Type: application/json" \
            -w "\nHTTP Status: %{http_code}\n" \
            -o response.json
          
          echo "Response:"
          cat response.json
          
          # Check if request was successful
          if [ $(jq -r '.success' response.json) != "true" ]; then
            echo "Analytics agent failed"
            exit 1
          fi
          
      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: analytics-agent-results-${{ github.run_id }}
          path: response.json
          retention-days: 7

