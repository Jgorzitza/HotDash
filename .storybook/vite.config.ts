import { defineConfig } from "vite";

// Minimal Vite config for Storybook to avoid loading project root vite.config.ts
export default defineConfig({
  // Intentionally minimal; Storybook will inject necessary plugins
});

